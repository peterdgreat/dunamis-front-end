import React from 'react';

import ImageUpload from './ImageUpload';

export default {
  title: 'Component/ImageUpload',
  component: ImageUpload,
};

const Template = () => <ImageUpload />;

export const imageupload = Template.bind({});
